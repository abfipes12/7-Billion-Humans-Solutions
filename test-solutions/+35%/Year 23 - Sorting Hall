-- 7 Billion Humans (2235) --
-- 23: Sorting Hall by @n05ucc4u --
-- failed ? out of ? => ?% successful --
-- n05ucc4u got 14s with it so its 14-15s --
-- size: 23 ; speed: 14-15s --

pickup s
if myitem > 49:
	a:
	if e < myitem:
		b:
		step e
		jump a
	endif
	c:
	if w > myitem:
		d:
		step w
		jump c
	endif
	if e < myitem:
		jump b
	endif
	if w > myitem:
		jump d
	endif
else:
	e:
	if w > myitem:
		f:
		step w
		jump e
	endif
	g:
	if e < myitem:
		h:
		step e
		jump g
	endif
	if w > myitem:
		jump f
	endif
	if e < myitem:
		jump h
	endif
endif